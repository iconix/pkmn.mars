import * as React from "react";

import {Constants} from "../constants";

export function Field() {
    // presentational component: functional, stateless
    return (
        <div className={Constants._.Classes.field}></div>
    );
}
